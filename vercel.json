{
    "functions": {
        "api/**/*.go": {
            "runtime": "vercel-plugin-go@0.2.0"
        }
    },
    "routes": [
        { "handle": "filesystem" },
        { "src": "/api/.*", "dest": "/api/index" },
        { "src": "/(.*)", "dest": "/public/$1" }
    ]
}
